---
title: "Run Your First Task"
description: "Complete your first Workbench task and inspect the resulting Work record"
---

## Overview

Use this first task to validate the Workbench loop: prompt -> run -> Work record
-> diff review -> revise/finalize.

If you are starting from the terminal, begin with [Work Records](/work-records)
to register a workspace, inspect schemas and local records, enable best-effort
`git`/`gh` capture, or link a PR. This page focuses on the desktop Workbench
task flow.

## Recommended first task type

Pick a small, low-risk change:

- update a UI label
- improve one validation message
- fix one obvious bug in a narrow file scope

## Step-by-step

1. Open a workspace.
2. Create a task with one clear objective.
3. Choose harness/model in composer.
4. Send a focused first prompt with acceptance criteria.
5. Let the session run to completion.
6. Review the transcript, artifacts, and diff carefully.
7. If behavior matters more than code shape, ask for an artifact such as a screenshot or short report.
8. Use `ctx work search`, `ctx work context`, or `ctx work report` if you want a CLI view of a first-class Work Record.
9. Request revision or finalize.

## First prompt template

```text
Goal: <one concrete outcome>
Constraints: <what must not change>
Validation: <tests or checks to run>
Output: summarize the changed files and why
```

Example:

```text
Goal: fix login form validation for empty email.
Constraints: keep the current UI copy and styling.
Validation: run the related tests.
Output: summarize the changed files and rationale.
```

## Helpful composer controls

- Use `@` to reference files and directories.
- Use `/` to trigger harness-supported slash commands.

## What success looks like

- Diff matches task scope.
- No unrelated file churn.
- The Work record keeps the useful context: prompt, transcript, artifacts, diff, checks, and source-control links when available.
- Requested validation is addressed.
- You can decide confidently in review.

## Related pages

- [Tasks](/workbench/tasks)
- [Sessions and Messages](/workbench/sessions)
- [Review and Apply Changes](/workbench/review-and-apply-changes)
- [Tips for Success with Agents](/quick-tips-for-success-with-agents)
